# Battle icon assets

Place small transparent PNG battle icons here.

Recommended files:

- `icon_pause.png`
- `icon_settings.png`

For text-like controls such as `x2` and `Auto`, prefer Phaser-rendered text instead of baked image text.
